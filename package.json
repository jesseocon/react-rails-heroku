{
  "name": "react-rails-heroku-app",
  "description": "test application to deploy react on rails app to heroku",
  "version": "1.0.0",
  "engines": {
    "node": "6.9.4"
  },
  "scripts": {
    "postinstall": "cd client && npm install && npm run build"
  }
}

